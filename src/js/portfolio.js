document.addEventListener('DOMContentLoaded', function () {
  const typeList = document.querySelector('.portfolio_types');

  typeList.addEventListener('click', e => {
    console.log(e.currentTarget);
  })


});